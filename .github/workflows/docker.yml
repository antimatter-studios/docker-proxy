name: Docker

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - '*.*.*'
    paths-ignore:
      - '.gitignore'
      - 'LICENSE'
      - '*.md'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  registry: ghcr.io

# One publish at a time per branch or tag, in the order they were started, and never
# cancelled halfway. Each run builds the commit its ref pointed at when it was started,
# so on main the run that finishes last is always the newest and `latest` cannot roll
# back to an older image. Tags get a group of their own: GitHub keeps only one pending
# run per group, and a release waiting behind a main build must not be dropped when a
# newer main build arrives.
concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  multiarch-build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Get Docker tags
        id: docker_meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.registry }}/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
          labels: |
            org.opencontainers.image.authors=Chris Thomas <chris.thomas@antimatter-studios.com> (@chrisalexthomas)
            org.opencontainers.image.version=${{ env.GIT_DESCRIBE }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.registry }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push the image
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          push: true
          tags: ${{ steps.docker_meta.outputs.tags }}
          labels: ${{ steps.docker_meta.outputs.labels }}

      - name: Images digests
        run: echo ${{ steps.docker_build.outputs.digest }}
